# Set boot menu timeout to 0 seconds (boot immediately)
set timeout=0

# Set the first (and only) entry as default
set default=0

# Boot entry for your kernel
menuentry "KFS1 Kernel" {
    multiboot /boot/kernel.bin   # Tell GRUB to load a Multiboot-compatible kernel
    boot                         # Start the boot process
}
